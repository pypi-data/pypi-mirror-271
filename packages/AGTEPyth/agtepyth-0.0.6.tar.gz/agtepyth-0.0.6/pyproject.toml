[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "AGTEPyth"
description = "A sample python package to start sharing your code with the world"
version = "0.0.6"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
                "pandas", "numpy", "matplotlib", "pyqt5", "python-docx"
                ]