[project]
name = "deksound"
version = "0.1.1"
description = ""
authors = [
    { name = "sanzenwin", email = "sanzenwin@gmail.com" },
]
dependencies = [
    "typer<=0.9.0",
    "just-playback<=0.1.8",
]
requires-python = ">=3.10"
readme = "README.md"

[project.license]
text = "MIT"

[project.scripts]
deksound = "deksound.click.__entry__:main"

[tool.pdm.build]
includes = [
    "deksound",
]

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"
