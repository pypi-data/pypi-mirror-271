[tool.poetry]
name = "demo-py-lib"
version = "0.1.0"
description = "A demo test lib"
authors = ["Luigi Corollo <corollo@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
