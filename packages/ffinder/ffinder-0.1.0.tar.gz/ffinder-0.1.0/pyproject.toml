[tool.poetry]
name = "ffinder"
version = "0.1.0"
description = ""
authors = ["Miguel Freitas <miguel_flj@id.uff.br>"]
readme = "README.md"
packages = [{include = "file_finder"}]

[tool.poetry.dependencies]
python = "^3.11"
click = "^8.1.7"
tabulate = "^0.9.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
ffinder = "file_finder.cli:cli"