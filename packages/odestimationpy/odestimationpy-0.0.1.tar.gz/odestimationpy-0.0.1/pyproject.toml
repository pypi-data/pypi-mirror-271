[build-system]
requires = ["setuptools>=61.0", "matplotlib", "numpy", "scipy"]

build-backend = "setuptools.build_meta"

[project]
name = "odestimationpy"
version = "0.0.1"
authors = [
  { name="Nahomi Bouza", email="nahomi.bouza@gmail.com" },
]
description = "Estimate the parameters of a system of ordinary differential equations that are linear with respect to the parameters"
readme = "README.md"
requires-python = ">=3.8"

[project.urls]
Homepage = "https://github.com/NahomiB/ode-estimation"