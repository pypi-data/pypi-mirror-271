[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "query-test-lib"
version = "0.0.1"
authors = [
    {name = "Ankit Jangid"},
]
description = "Query Service Package"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests",
    'importlib-metadata; python_version<"3.10"',
]
