[tool.poetry]
name = "sb_utils"
version = "0.1.0"
description = "A collection of framework specific utilities for working with Supabase."
authors = ["Basri <h@basri.me>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
supabase = "^2.4.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
